services:
  crawler:
    build: .
    environment:
      - POSTGRES_HOST=${POSTGRES_HOST:-host.docker.internal}
      - POSTGRES_PORT=${POSTGRES_PORT:-5432}
      - POSTGRES_DB=${POSTGRES_DB:-postgres}
      - POSTGRES_USER=${POSTGRES_USER:-postgres}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-postgres}
      - MINIO_ENDPOINT=${MINIO_ENDPOINT:-}
      - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY:-}
      - MINIO_SECRET_KEY=${MINIO_SECRET_KEY:-}
      - MINIO_BUCKET=${MINIO_BUCKET:-drug-predicate-documents}
      - TEST_MODE=${TEST_MODE:-false}
      - MAX_RECORDS_PER_COUNTRY=${MAX_RECORDS_PER_COUNTRY:-0}
      # One-off maintenance paths (see app/main.py) — run INSTEAD of the
      # normal crawl loop when set, so they must be unset again afterwards.
      - FDA_BACKFILL_LABELS=${FDA_BACKFILL_LABELS:-false}
      - FDA_BACKFILL_DRY_RUN=${FDA_BACKFILL_DRY_RUN:-false}
      - UK_BACKFILL_REGISTRATION_NUMBERS=${UK_BACKFILL_REGISTRATION_NUMBERS:-false}
      - UK_BACKFILL_APPLY=${UK_BACKFILL_APPLY:-false}
      - UK_BACKFILL_LIMIT=${UK_BACKFILL_LIMIT:-}
    volumes:
      # Caches the FDA bulk-data zip so repeated runs don't re-download it
      # (which trips www.fda.gov's abuse detection). See the README.
      - ./.cache:/app/.cache
